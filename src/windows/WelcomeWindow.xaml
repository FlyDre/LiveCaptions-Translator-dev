<ui:FluentWindow
    x:Class="LiveCaptionsTranslator.WelcomeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LiveCaptionsTranslator"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="快速开始"
    Width="700"
    Height="490"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ExtendsContentIntoTitleBar="True"
    ResizeMode="NoResize"
    WindowBackdropType="Mica"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar
            Grid.Row="0"
            Height="27"
            ShowMaximize="False"
            ShowMinimize="False">
            <ui:TitleBar.Header>
                <ui:TextBlock Padding="15,0,0,0" VerticalAlignment="Center">
                    快速开始
                </ui:TextBlock>
            </ui:TitleBar.Header>
        </ui:TitleBar>

        <StackPanel Grid.Row="1" Background="{ui:ThemeResource SystemFillColorNeutralBackgroundBrush}">
            <TextBlock
                Margin="20"
                HorizontalAlignment="Left"
                FontSize="24"
                FontWeight="Bold"
                Text="欢迎使用 LiveCaptions Translator！"
                TextAlignment="Left" />
            <ui:TextBlock
                Margin="20,10,20,10"
                TextAlignment="Left"
                TextWrapping="Wrap">
                <Run Text="为确保开箱即用，请按以下步骤完成本地环境准备：&#x0A;" />
                <Run Text="&#x0A;1. 在发布目录中手动运行 " />
                <Run FontWeight="Bold" Text="OllamaSetup.exe" />
                <Run Text="，完成 Ollama 安装。&#x0A;" />
                <Run Text="2. 双击运行脚本 " />
                <Run FontWeight="Bold" Text="scripts\\install_qwen2.5-3b.bat" />
                <Run Text="，自动拉取模型 " />
                <Run FontWeight="Bold" Text="qwen2.5:3b" />
                <Run Text="。&#x0A;" />
                <Run Text="3. 启动 " />
                <Run FontWeight="Bold" Text="LiveCaptionsTranslator-win-x64-withruntime.exe" />
                <Run Text=" 即可开始使用。&#x0A;" />
                <Run FontWeight="Bold" Text="&#x0A;提示：" />
                <Run Text="如果你已安装 Ollama，可直接执行第 2 步。&#x0A;" />
                <Run Text="此外请确保 Windows LiveCaptions 可正常使用，并在其中设置好源语言。&#x0A;" />
                <Run Text="&#x0A;更多说明请查看 Wiki： " />
                <Hyperlink NavigateUri="https://github.com/SakiRinn/LiveCaptions-Translator/wiki" RequestNavigate="Hyperlink_RequestNavigate">
                    https://github.com/SakiRinn/LiveCaptions-Translator/wiki
                </Hyperlink>
            </ui:TextBlock>

            <ui:Button
                Margin="0,10,0,0"
                Padding="30,10,30,10"
                HorizontalAlignment="Center"
                Click="CloseButton_Click"
                Content="我已完成以上步骤" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
